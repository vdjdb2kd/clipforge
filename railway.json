{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "RAILPACK"
  },
  "deploy": {
    "startCommand": "gunicorn main:app --bind 0.0.0.0:$PORT --timeout 600 --workers 1",
    "restartPolicyType": "ON_FAILURE"
  }
}
